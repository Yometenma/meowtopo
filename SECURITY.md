# 安全政策

请通过 GitHub Security Advisory 私下报告命令注入、任意文件访问、跨站脚本、扫描范围绕过或备份恢复问题，不要在公开 Issue 中披露可利用细节。

MoeTopo 默认监听 `127.0.0.1:8088`；Compose 为家庭局域网使用监听所有地址。不要将服务直接暴露到互联网。项目当前没有身份认证，信任边界是家庭 LAN。恢复备份前请自行保留当前数据副本。
