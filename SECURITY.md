# 安全政策

请通过 GitHub Security Advisory 私下报告命令注入、任意文件访问、跨站脚本、扫描范围绕过或备份恢复问题，不要在公开 Issue 中披露可利用细节。

MeowTopo 默认监听 `127.0.0.1:8088`；Compose 为家庭局域网使用监听所有地址。首次打开必须创建管理员账户，管理员可以为家庭成员或朋友创建权限受限的本地账户。密码只保存为带随机盐的摘要，登录 Cookie 不允许网页脚本读取，修改操作还会校验页面令牌。

账户保护不能代替安全的部署方式。不要将服务端口直接暴露到互联网；远程使用优先选择可信 VPN，经过反向代理时应启用 HTTPS。请为管理员设置独立的强密码，并及时停用不再需要的账户。

数据库备份包含家庭设备资料、账户密码摘要和可能仍然有效的登录记录，应像密码文件一样妥善保存。恢复备份前请自行保留当前数据副本，恢复后如怀疑备份泄露，应重设账户密码。
